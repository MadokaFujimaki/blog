@using PizzeriaEpiserverSite.Helpers
@using EPiServer.Core
@using EPiServer
@using PizzeriaEpiserverSite.Business
@using PizzeriaEpiserverSite.Models.Pages
@using PizzeriaEpiserverSite.Models.ViewModels
@using PizzeriaEpiserverSite.Extensions
@model IPageViewModel<SitePageData>



<div class="navbar navbar-inverse navbar-fixed-top">
        <div id="navbarContainer" class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>       
                          
                @*<h1> @ContentReference.StartPage.GetUrl()</h1>
                <h1>@Url.ContentUrl(ContentReference.StartPage)</h1>*@
                <a href="/@ContentReference.StartPage.GetUrl()"><img id="logo" src="/Static/img/d0088452_19251252.gif" /></a>

            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav navbar-left">
                    @*<li class="@(Model.CurrentPage.ContentLink.CompareToIgnoreWorkID(ContentReference.StartPage) ? "active" : null)">
                    @Html.PageLink(ContentReference.StartPage)
                </li>*@

                    @Html.MenuList(ContentReference.StartPage,
                @<li class="@(item.Selected ? "active" : null)">
                    @Html.PageLink(item.Page)
                </li>)
                </ul>
                <div class="navbar-search">
                    @using (Html.BeginForm(null, null, FormMethod.Post, new { @action = DataFactory.Instance.Get<PizzeriaEpiserverSite.Models.Pages.StartPage>(PageReference.StartPage).SearchPageLink.ExternalURLFromReference() }))
                    {
                        <div class="col-sm-3 col-md-3 pull-right" >
                            <form class="navbar-form" role="search">
                                <div class="input-group">
                                    <input class="form-control" type="text" tabindex="1" name="q" id="SearchKeywords" placeholder="Search" />
                                    <div class="input-group-btn">
                                        <button tabindex="2" class="btn" type="submit"><i class="glyphicon glyphicon-search" id="SearchButton"></i></button>
                                    </div>
                                </div>
                            </form>
                        </div>
                    }
                </div>

            </div>
        </div>
    </div>








