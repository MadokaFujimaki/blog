@using PizzeriaEpiserverSite.Models.ViewModels
@model BlogCategoryListViewModel

@*@foreach (var page in Model.Pages)
{
    <h2>@page.Name</h2>
}*@

@*@foreach (var category in Model.Categories)
{
    <h3>@category.Name</h3>
}*@

<div class="row">
    <div class="col-md-3">
        <p>Categories</p>
        @foreach (var Category in Model.Categories)
        {
            <a href="@Url.Action("Index", "BlogCategoryListPage", new { category = Category.Name })">@Category.Name</a>
            <br />
        }
    </div>

    <div class="col-md-9">
        @Html.Partial("Blogs")
    </div>
</div>




@*@if (Model.category != null)
{
    foreach (var item in Model.category)
    {
        <div class="listResult theme1">
            <h3>@Html.PageLink(item)</h3>
            <p class="date">
                @item.StartPublish.Value.ToString("d MMMM yyyy")
            </p>
            @if (item.Property["MainBody"] != null)
            {
                @item.Property["MainBody"].Value
            }
            <hr />
        </div>
    }*@
